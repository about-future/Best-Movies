<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingEnd="@dimen/details_horizontal_spacing"
    android:paddingStart="@dimen/activity_horizontal_margin">

    <ImageView
        android:id="@+id/actor_profile_iv"
        android:layout_width="100dp"
        android:layout_height="150dp"
        android:background="@color/colorPrimaryDark"
        android:contentDescription="@string/movie_poster"
        android:scaleType="centerCrop"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:srcCompat="@drawable/ic_person" />

    <TextView
        android:id="@+id/actor_name_tv"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/touch_selector"
        android:fontFamily="sans-serif-condensed"
        android:gravity="bottom"
        android:textAlignment="center"
        android:textColor="@color/colorWhite"
        android:textSize="@dimen/cast_member_name_size"
        app:layout_constraintEnd_toEndOf="@id/actor_profile_iv"
        app:layout_constraintStart_toStartOf="@id/actor_profile_iv"
        app:layout_constraintTop_toBottomOf="@id/actor_profile_iv"
        tools:text="Name and Surname" />

    <TextView
        android:id="@+id/character_tv"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@drawable/touch_selector"
        android:fontFamily="sans-serif-condensed"
        android:gravity="bottom"
        android:textAlignment="center"
        android:textColor="@color/colorAccent"
        android:textSize="@dimen/cast_character_name_size"
        app:layout_constraintEnd_toEndOf="@id/actor_profile_iv"
        app:layout_constraintStart_toStartOf="@id/actor_profile_iv"
        app:layout_constraintTop_toBottomOf="@id/actor_name_tv"
        tools:text="Character" />

</android.support.constraint.ConstraintLayout>